<div class="min-h-screen bg-gray-50 flex flex-col">
  <!-- Navigation -->
  <nav class="bg-white border-b sticky top-0 z-40 safe-area-inset-top">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 py-3 flex justify-between items-center">
      <.link navigate={~p"/"} class="text-xl font-bold text-emerald-600 flex items-center gap-2">
        <div class="w-8 h-8 bg-emerald-600 rounded-lg flex items-center justify-center">
          <.icon name="hero-microphone" class="w-4 h-4 text-white" />
        </div>
        <span class="hidden sm:inline">ScriptVoice</span>
      </.link>

      <div class="flex items-center gap-2 sm:gap-4">
        <.link navigate={~p"/browse"} class="text-gray-600 hover:text-gray-900 font-medium text-sm sm:text-base px-2 py-1">
          Scripts
        </.link>
        <.link navigate={~p"/collectives"} class="text-gray-600 hover:text-gray-900 font-medium text-sm sm:text-base px-2 py-1">
          Collectives
        </.link>

        <%= if assigns[:current_user] do %>
          <.link navigate={~p"/dashboard"} class="text-gray-600 hover:text-gray-900 font-medium text-sm sm:text-base px-2 py-1">
            Dashboard
          </.link>
          <!-- User Menu with Dropdown -->
          <div class="relative" id="user-menu">
            <button
              type="button"
              class="flex items-center gap-2 bg-gray-100 hover:bg-gray-200 px-3 py-1.5 rounded-full transition"
              onclick="document.getElementById('user-dropdown').classList.toggle('hidden')"
            >
              <%= if @current_user.verification_status == "verified" do %>
                <.verified_badge />
              <% end %>
              <span class="text-sm font-medium truncate max-w-[100px] sm:max-w-none">
                <%= @current_user.name %>
              </span>
              <.icon name="hero-chevron-down" class="w-4 h-4 text-gray-500" />
            </button>

            <!-- Dropdown Menu -->
            <div id="user-dropdown" class="hidden absolute right-0 mt-2 w-56 bg-white rounded-xl shadow-lg border py-2 z-50">
              <!-- Current Role -->
              <div class="px-4 py-2 border-b">
                <p class="text-xs text-gray-500">Signed in as</p>
                <p class="font-medium text-sm">
                  <%= case @current_user.user_type do
                    "writer" -> "Writer"
                    "voice_artist" -> "Voice Artist"
                    _ -> "Visitor"
                  end %>
                </p>
              </div>

              <!-- Quick Links -->
              <div class="py-2 border-b">
                <.link
                  navigate={~p"/dashboard"}
                  class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-50"
                >
                  <.icon name="hero-home" class="w-4 h-4 text-gray-400" />
                  Dashboard
                </.link>
                <.link
                  navigate={~p"/dashboard?tab=profile"}
                  class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-50"
                >
                  <.icon name="hero-user" class="w-4 h-4 text-gray-400" />
                  My Profile
                </.link>
                <.link
                  navigate={~p"/dashboard?tab=commissions"}
                  class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-50"
                >
                  <.icon name="hero-clipboard-document-list" class="w-4 h-4 text-gray-400" />
                  Commissions
                </.link>
                <%= if @current_user.user_type == "voice_artist" do %>
                  <.link
                    navigate={~p"/dashboard?tab=collectives"}
                    class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-50"
                  >
                    <.icon name="hero-user-group" class="w-4 h-4 text-gray-400" />
                    My Collectives
                  </.link>
                  <.link
                    navigate={~p"/settings/pricing"}
                    class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-50"
                  >
                    <.icon name="hero-currency-dollar" class="w-4 h-4 text-gray-400" />
                    Pricing Settings
                  </.link>
                <% end %>
              </div>

              <!-- Logout -->
              <div class="py-2">
                <.link
                  href={~p"/session"}
                  method="delete"
                  class="flex items-center gap-3 px-4 py-2 text-sm text-red-600 hover:bg-red-50"
                >
                  <.icon name="hero-arrow-right-on-rectangle" class="w-4 h-4" />
                  Sign Out
                </.link>
              </div>
            </div>
          </div>
        <% else %>
          <.link navigate={~p"/verify"} class="bg-emerald-600 text-white px-3 sm:px-4 py-1.5 sm:py-2 rounded-lg text-sm font-medium hover:bg-emerald-700 transition">
            Get Started
          </.link>
        <% end %>
      </div>
    </div>
  </nav>

  <!-- Click outside to close dropdown -->
  <script>
    document.addEventListener('click', function(e) {
      var dropdown = document.getElementById('user-dropdown');
      var menu = document.getElementById('user-menu');
      if (dropdown && menu && !menu.contains(e.target)) {
        dropdown.classList.add('hidden');
      }
    });
  </script>

  <!-- Main Content -->
  <main class="flex-1">
    <.flash_group flash={@flash} />
    <%= @inner_content %>
  </main>
</div>
